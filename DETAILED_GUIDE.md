# 实时汇率转换器 - 详细说明文档

## 📋 目录

- [工具概述](#工具概述)
- [核心功能](#核心功能)
- [技术架构](#技术架构)
- [安装方法](#安装方法)
- [使用指南](#使用指南)
- [功能详解](#功能详解)
- [支持的货币](#支持的货币)
- [常见问题](#常见问题)
- [开发说明](#开发说明)

---

## 🎯 工具概述

**实时汇率转换器**是一款智能的 Chrome 浏览器扩展，能够自动识别网页中的货币金额，并实时转换为你的本地货币。无需手动计算，让跨境购物、阅读国际新闻、查看海外产品价格变得轻松便捷。

### 适用场景

- 🛒 **跨境电商购物**：在 Amazon、eBay 等海外电商平台购物时，自动显示人民币价格
- 📰 **阅读国际新闻**：浏览 BBC、CNN 等外媒时，快速了解金额的实际价值
- 💼 **商务办公**：查看国际报价、合同金额时，即时转换为本地货币
- 🎮 **游戏充值**：Steam、PlayStation 等平台购买游戏时，清楚了解实际花费
- ✈️ **旅游规划**：预订酒店、机票时，直观对比不同货币的价格

### 核心优势

1. **智能识别**：自动检测页面中的货币符号和金额，无需手动选择
2. **实时转换**：使用最新汇率数据，确保转换结果准确
3. **优雅设计**：采用 shadcn/ui 设计系统，界面简洁美观
4. **多语言支持**：支持中文和英文界面切换
5. **隐私保护**：所有数据本地存储，不上传任何个人信息
6. **开源免费**：完全开源，永久免费使用

---

## ⚡ 核心功能

### 1. 自动货币识别

扩展能够智能识别以下格式的货币金额：

- **符号格式**：`$99.99`、`€49.99`、`£79.99`、`¥699`
- **代码格式**：`USD 99.99`、`EUR 49.99`、`GBP 79.99`
- **混合格式**：`99.99 USD`、`49.99 EUR`
- **中文格式**：`99.99美元`、`49.99欧元`

支持的货币符号：
- `$` - 美元 (USD)
- `€` - 欧元 (EUR)
- `£` - 英镑 (GBP)
- `¥` - 日元/人民币 (JPY/CNY)
- `₩` - 韩元 (KRW)
- `HK$` - 港币 (HKD)

### 2. 实时汇率转换

- **数据来源**：使用 ExchangeRate-API 提供的实时汇率数据
- **更新频率**：汇率数据每小时自动更新一次
- **缓存机制**：本地缓存汇率数据，减少 API 请求，提升响应速度
- **精确度**：支持 2-4 位小数，满足不同精度需求

### 3. 可视化展示

转换后的金额以**绿色渐变卡片**形式显示在原金额旁边：

```
原始金额：$99.99
转换显示：≈ ¥699.93  (绿色高亮)
```

**悬停交互**：
- 鼠标悬停时显示详细汇率信息
- 闪光动画效果，提升视觉体验
- 使用等宽字体，数字对齐美观

### 4. 智能触发机制

扩展会在以下情况自动执行转换：

1. **页面加载完成**：打开新页面时自动扫描
2. **标签页切换**：切换到其他标签页时重新扫描
3. **动态内容加载**：检测 AJAX、SPA 等动态内容变化
4. **设置变更**：修改设置后立即应用，无需刷新页面
5. **扩展重载**：更新或重载扩展后自动重新初始化

### 5. 侧边栏控制面板

点击浏览器工具栏的扩展图标，打开侧边栏控制面板：

#### 功能控制
- **自动转换开关**：一键启用/禁用货币转换功能
- **实时生效**：开关状态立即应用到当前页面

#### 转换设置
- **本地货币选择**：支持 CNY、USD、EUR、GBP、JPY、KRW、HKD 等主流货币
- **小数位数**：可设置 2-4 位小数精度
- **显示汇率信息**：选择是否在悬停时显示详细汇率

#### 汇率状态
- **实时状态显示**：显示汇率数据的更新时间
- **缓存管理**：一键清除缓存，强制刷新汇率数据

#### 语言切换
- **中英文切换**：点击右上角按钮切换界面语言
- **即时生效**：语言切换后立即更新所有文本

---

## 🏗️ 技术架构

### 技术栈

- **核心框架**：Chrome Extension Manifest V3
- **开发语言**：TypeScript
- **构建工具**：Webpack 5
- **测试框架**：Jest + fast-check (属性测试)
- **UI 设计**：shadcn/ui 设计系统
- **API 服务**：ExchangeRate-API

### 模块结构

```
src/
├── background.ts        # 后台服务 Worker
├── content.ts          # 内容脚本 (页面注入)
├── popup.ts            # 侧边栏 UI 逻辑
├── detector.ts         # 货币检测模块
├── converter.ts        # 货币转换模块
├── rateProvider.ts     # 汇率数据提供者
├── settingsManager.ts  # 设置管理模块
├── overlay.ts          # 覆盖层 UI 模块
├── i18n.ts            # 国际化模块
├── types.ts           # TypeScript 类型定义
└── config.ts          # 配置文件
```

### 工作流程

```
1. 用户打开网页
   ↓
2. content.ts 注入到页面
   ↓
3. detector.ts 扫描页面，识别货币金额
   ↓
4. background.ts 获取汇率数据 (缓存优先)
   ↓
5. converter.ts 执行货币转换计算
   ↓
6. overlay.ts 在页面上渲染转换结果
   ↓
7. 用户悬停查看详细信息
```

### 数据流

```
ExchangeRate-API
      ↓
rateProvider.ts (获取汇率)
      ↓
Chrome Storage (本地缓存)
      ↓
converter.ts (转换计算)
      ↓
overlay.ts (UI 渲染)
```

---

## 📦 安装方法

### 方法一：从 GitHub Releases 下载 (推荐)

1. 访问 [GitHub Releases](https://github.com/xcondor/currency-converter-extension/releases)
2. 下载最新版本的 `currency-converter.crx` 文件
3. 打开 Chrome 浏览器，访问 `chrome://extensions/`
4. 开启右上角的"开发者模式"
5. 将 `.crx` 文件拖拽到扩展管理页面
6. 点击"添加扩展程序"确认安装

### 方法二：从源码安装

1. **克隆仓库**
   ```bash
   git clone https://github.com/xcondor/currency-converter-extension.git
   cd currency-converter-extension
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **构建项目**
   ```bash
   npm run build
   ```

4. **加载扩展**
   - 打开 Chrome 浏览器，访问 `chrome://extensions/`
   - 开启"开发者模式"
   - 点击"加载已解压的扩展程序"
   - 选择项目中的 `dist` 目录

### 方法三：Chrome Web Store (即将上线)

待扩展通过 Chrome Web Store 审核后，可直接在商店搜索"实时汇率转换器"安装。

---

## 📖 使用指南

### 快速开始

1. **安装扩展**：按照上述方法安装扩展
2. **打开侧边栏**：点击浏览器工具栏的扩展图标 💱
3. **设置本地货币**：在"转换设置"中选择你的本地货币（默认：CNY）
4. **启用自动转换**：确保"自动转换"开关处于开启状态
5. **浏览网页**：访问任何包含货币金额的网页，自动显示转换结果

### 基础操作

#### 启用/禁用转换

- 打开侧边栏
- 切换"功能控制"卡片中的"自动转换"开关
- 当前页面立即生效，无需刷新

#### 更改本地货币

- 打开侧边栏
- 在"转换设置"中选择"本地货币"下拉菜单
- 选择你想要转换到的目标货币
- 设置自动保存并立即生效

#### 调整小数精度

- 打开侧边栏
- 在"转换设置"中修改"小数位数"（2-4 位）
- 适用于不同货币的精度需求

#### 查看汇率信息

- 勾选"悬停时显示汇率信息"选项
- 鼠标悬停在转换后的金额上
- 显示详细的汇率和转换时间

#### 清除缓存

- 打开侧边栏
- 点击"清除缓存"按钮
- 强制刷新汇率数据

#### 切换语言

- 点击侧边栏右上角的语言按钮
- 中文模式显示"EN"，英文模式显示"中文"
- 点击即可切换界面语言

### 高级技巧

#### 1. 针对特定网站禁用

如果某些网站的货币转换效果不理想，可以：
- 在该网站上打开侧边栏
- 关闭"自动转换"开关
- 切换到其他网站时再重新开启

#### 2. 快速刷新汇率

如果发现汇率数据过时：
- 打开侧边栏
- 查看"汇率状态"显示的更新时间
- 点击"清除缓存"强制刷新

#### 3. 多货币对比

在同一页面上可能出现多种货币：
- 扩展会自动识别所有货币类型
- 统一转换为你设置的本地货币
- 方便横向对比价格

---

## 💰 支持的货币

扩展支持 **160+ 种全球货币**，包括但不限于：

### 主流货币

| 货币代码 | 货币名称 | 符号 |
|---------|---------|------|
| CNY | 人民币 | ¥ |
| USD | 美元 | $ |
| EUR | 欧元 | € |
| GBP | 英镑 | £ |
| JPY | 日元 | ¥ |
| KRW | 韩元 | ₩ |
| HKD | 港币 | HK$ |
| AUD | 澳元 | A$ |
| CAD | 加元 | C$ |
| SGD | 新加坡元 | S$ |

### 亚洲货币

- TWD (新台币)
- THB (泰铢)
- MYR (马来西亚林吉特)
- IDR (印尼盾)
- PHP (菲律宾比索)
- VND (越南盾)
- INR (印度卢比)

### 欧洲货币

- CHF (瑞士法郎)
- SEK (瑞典克朗)
- NOK (挪威克朗)
- DKK (丹麦克朗)
- PLN (波兰兹罗提)
- RUB (俄罗斯卢布)

### 美洲货币

- MXN (墨西哥比索)
- BRL (巴西雷亚尔)
- ARS (阿根廷比索)
- CLP (智利比索)

### 中东/非洲货币

- AED (阿联酋迪拉姆)
- SAR (沙特里亚尔)
- ZAR (南非兰特)
- EGP (埃及镑)

完整货币列表请访问 [ExchangeRate-API 支持的货币](https://www.exchangerate-api.com/docs/supported-currencies)

---

## ❓ 常见问题

### Q1: 为什么有些金额没有被识别？

**可能原因：**
- 金额格式不标准（如缺少货币符号或代码）
- 金额被 JavaScript 动态生成，加载时机晚于扫描
- 金额在 iframe 或 Shadow DOM 中

**解决方法：**
- 等待页面完全加载后再查看
- 切换到其他标签页再切换回来，触发重新扫描
- 关闭并重新打开"自动转换"开关

### Q2: 转换结果不准确怎么办？

**可能原因：**
- 汇率数据缓存过期
- 原始金额识别错误

**解决方法：**
- 打开侧边栏，点击"清除缓存"刷新汇率
- 检查"汇率状态"显示的更新时间
- 确认原始金额格式是否标准

### Q3: 扩展图标不显示？

**可能原因：**
- 浏览器缓存问题
- 扩展安装不完整

**解决方法：**
- 访问 `chrome://extensions/`
- 找到"实时汇率转换器"
- 点击"重新加载"按钮
- 刷新当前网页

### Q4: 如何在无痕模式下使用？

**操作步骤：**
1. 访问 `chrome://extensions/`
2. 找到"实时汇率转换器"
3. 点击"详细信息"
4. 开启"在无痕模式下启用"

### Q5: 扩展会收集我的隐私数据吗？

**隐私保护承诺：**
- ✅ 所有设置和汇率数据存储在本地
- ✅ 不上传任何浏览历史或个人信息
- ✅ 仅向 ExchangeRate-API 请求汇率数据
- ✅ 完全开源，代码可审计

### Q6: 支持哪些浏览器？

**官方支持：**
- ✅ Google Chrome (推荐)
- ✅ Microsoft Edge
- ✅ Brave
- ✅ 其他基于 Chromium 的浏览器

**不支持：**
- ❌ Firefox (需要单独适配)
- ❌ Safari (需要单独适配)

### Q7: 如何更新扩展？

**从 GitHub 安装：**
- 下载最新版本的 `.crx` 文件
- 重新拖拽安装即可覆盖旧版本

**从源码安装：**
```bash
git pull origin main
npm install
npm run build
```
然后在 `chrome://extensions/` 点击"重新加载"

### Q8: 转换速度慢怎么办？

**优化建议：**
- 首次访问网站时需要获取汇率数据，可能稍慢
- 后续访问会使用缓存，速度会明显提升
- 如果页面金额过多（>100 个），扫描可能需要几秒钟
- 可以暂时关闭"自动转换"，需要时再开启

---

## 🛠️ 开发说明

### 环境要求

- Node.js >= 16.x
- npm >= 8.x
- Chrome >= 88

### 开发命令

```bash
# 安装依赖
npm install

# 开发模式 (自动重新编译)
npm run dev

# 生产构建
npm run build

# 运行测试
npm test

# 测试覆盖率
npm run test:coverage

# 类型检查
npm run lint

# 生成图标
npm run generate-icons

# 生成宣传图片
npm run generate-promo
```

### 项目结构

```
currency-converter-extension/
├── src/                    # 源代码
│   ├── background.ts       # 后台服务
│   ├── content.ts         # 内容脚本
│   ├── popup.ts           # 侧边栏 UI
│   ├── detector.ts        # 货币检测
│   ├── converter.ts       # 货币转换
│   ├── rateProvider.ts    # 汇率提供者
│   ├── settingsManager.ts # 设置管理
│   ├── overlay.ts         # 覆盖层 UI
│   ├── i18n.ts           # 国际化
│   ├── types.ts          # 类型定义
│   └── config.ts         # 配置
├── public/                # 静态资源
│   ├── manifest.json     # 扩展清单
│   ├── popup.html        # 侧边栏 HTML
│   ├── popup.css         # 侧边栏样式
│   └── icons/            # 图标文件
├── tests/                # 测试文件
├── scripts/              # 构建脚本
├── dist/                 # 构建输出
└── docs/                 # 文档
```

### 核心模块说明

#### 1. detector.ts - 货币检测模块

**功能：**
- 使用正则表达式识别页面中的货币金额
- 支持多种货币格式和符号
- 返回检测到的货币信息（金额、货币代码、位置）

**关键方法：**
```typescript
detectCurrencies(text: string): CurrencyMatch[]
```

#### 2. converter.ts - 货币转换模块

**功能：**
- 根据汇率数据执行货币转换
- 支持自定义小数精度
- 处理转换错误和边界情况

**关键方法：**
```typescript
convert(amount: number, from: string, to: string, rates: ExchangeRates): number
```

#### 3. rateProvider.ts - 汇率提供者

**功能：**
- 从 ExchangeRate-API 获取汇率数据
- 实现缓存机制（1 小时有效期）
- 处理 API 请求失败和重试

**关键方法：**
```typescript
getRates(baseCurrency: string): Promise<ExchangeRates>
```

#### 4. overlay.ts - 覆盖层 UI

**功能：**
- 在页面上渲染转换后的金额
- 实现悬停交互效果
- 管理覆盖层的生命周期

**关键方法：**
```typescript
createOverlay(match: CurrencyMatch, convertedAmount: number): HTMLElement
```

#### 5. i18n.ts - 国际化模块

**功能：**
- 管理中英文翻译
- 提供语言切换功能
- 同步语言设置到所有组件

**关键方法：**
```typescript
t(key: string): string
setLanguage(lang: Language): Promise<void>
```

### 测试策略

**单元测试：**
- 每个模块都有对应的测试文件
- 覆盖核心功能和边界情况
- 使用 Jest 作为测试框架

**属性测试：**
- 使用 fast-check 进行属性测试
- 验证货币转换的数学属性
- 确保转换结果的一致性

**测试覆盖率：**
- 目标：>80% 代码覆盖率
- 重点测试核心转换逻辑
- 定期运行测试确保质量

### 贡献指南

欢迎贡献代码！请参考 [CONTRIBUTING.md](CONTRIBUTING.md) 了解详细信息。

**贡献流程：**
1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 🙏 致谢

- [ExchangeRate-API](https://www.exchangerate-api.com/) - 提供汇率数据
- [shadcn/ui](https://ui.shadcn.com/) - UI 设计系统
- [TypeScript](https://www.typescriptlang.org/) - 类型安全
- [Webpack](https://webpack.js.org/) - 模块打包
- [Jest](https://jestjs.io/) - 测试框架

---

## 📞 联系方式

- **GitHub Issues**: [提交问题](https://github.com/xcondor/currency-converter-extension/issues)
- **Email**: your.email@example.com
- **项目主页**: https://github.com/xcondor/currency-converter-extension

---

## 🔄 更新日志

查看 [CHANGELOG.md](CHANGELOG.md) 了解版本更新历史。

---

**最后更新时间**: 2026-01-06
**当前版本**: v1.0.0
