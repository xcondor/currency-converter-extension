# 快速参考 - Chrome Web Store 上架

## 🚀 一句话总结

**关键问题**：Chrome 警告 `<all_urls>` 权限会导致深入审核

**解决方案**：提供详细说明，强调这是通用工具（类似 Google Translate），完全开源，隐私保护

---

## 📝 三个权限说明（直接复制粘贴）

### 1. Storage 权限（简短版）

```
本扩展需要 storage 权限来存储用户设置（货币选择、小数位数、语言等）和汇率缓存数据。所有数据仅存储在本地设备，不上传到任何服务器。用户可随时通过"清除缓存"按钮删除数据。
```

### 2. SidePanel 权限（简短版）

```
本扩展需要 sidePanel 权限来显示设置控制面板，提供功能开关、货币选择、小数精度调整、语言切换等功能。侧边栏提供了比传统 popup 更好的用户体验，用户可以在浏览网页的同时调整设置。
```

### 3. 主机权限（详细版 - 最重要！）

```
本扩展是通用货币转换工具，必须使用 <all_urls> 权限才能实现核心功能。

【为什么需要 <all_urls>】
本扩展需要在用户访问的任何网站上自动检测和转换货币金额。使用场景包括：电商购物（Amazon、eBay、淘宝等）、新闻阅读（BBC、CNN等）、社交媒体、企业官网等数百万个网站。由于无法预先列出所有可能包含货币信息的网站，必须使用 <all_urls> 权限。

【为什么不能使用 activeTab】
扩展需要在页面加载时自动运行并持续监控动态内容更新（AJAX、SPA），而非每次手动触发。用户期望打开任何网站都能自动转换货币，而不是频繁点击扩展图标。

【隐私和安全保证】
✅ 只读取页面可见文本来检测货币符号（$、€、£、¥），不访问密码、表单、Cookie等敏感信息
✅ 所有检测和转换都在本地完成，不上传任何网页内容或用户数据
✅ 唯一外部请求：向 ExchangeRate-API 获取汇率数据（仅发送货币代码如"CNY"）
✅ 完全开源：代码可在 GitHub 审计验证

【类似扩展】
Google Translate、Grammarly、Dark Reader 等知名扩展都使用 <all_urls> 权限提供通用功能。

GitHub: https://github.com/xcondor/currency-converter-extension
```

---

## 🎯 关键策略

1. **强调通用性**：这是通用工具，不是针对特定网站
2. **举例类似扩展**：Google Translate、Grammarly 都用 <all_urls>
3. **隐私保护**：强调开源、本地处理、不上传数据
4. **提供证据**：GitHub 链接，邀请审核代码

---

## ⏱️ 预期时间线

- **正常审核**：1-3 天
- **深入审核**：1-2 周（因为 <all_urls>）
- **被拒后回复**：1-3 天再次审核

---

## 📧 如果被拒，回复模板

```
尊敬的审核团队：

本扩展类似 Google Translate，需要在任何网站运行。
无法预先列出所有网站，必须使用 <all_urls>。

隐私保护：
- 只读取可见文本检测货币
- 本地处理，不上传数据
- 完全开源可审计

GitHub: https://github.com/xcondor/currency-converter-extension

期待回复，谢谢！
```

---

## ✅ 提交前最后检查

- [ ] 已构建：`npm run build`
- [ ] 已打包：`zip -r currency-converter-extension.zip dist/`
- [ ] 已上传 3 张截图
- [ ] 已填写 3 个权限说明（复制上面内容）
- [ ] 已设置隐私政策链接（GitHub README）
- [ ] 已准备好应对深入审核

---

**记住**：耐心 + 详细说明 = 通过审核 ✅
